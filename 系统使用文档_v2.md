# 锁妖塔量化交易系统 v2.0

> 最后更新: 2026-07-28
> GitHub: https://github.com/davidlu020106-tech/suoyao-quant
> 每小时自动运行于 GitHub Actions

---

## 一、系统概述

把99个顶级加密交易员的交易直觉炼成88个量化因子，对OKX山寨币做全量扫描、排名和推荐。

### 核心理念

```
88因子 × 99交易员 = 实时KOL投票
15分钟K线 + 日线 = 双周期过滤
ADX≥25 = 有趋势才做
TP1利润≥100% = 翻倍才做
大盘环境过滤 = 不逆势
```

### 运行频率

| 平台 | 频率 | 触发方式 |
|:----|:----|:---------|
| GitHub Actions | 每小时（整点过5分） | 自动 |
| 本地电脑 | 任意 | `python run_daily_picks.py --top 40` |

---

## 二、安装与配置

### 环境要求

- Python 3.9+
- 网络连接（访问OKX API）

### 安装依赖

```bash
pip install pandas numpy python-docx
```

### 配置API密钥

编辑 `quant_factors/local_config.py`：

```python
OKX_API_KEY = '你的API Key'
OKX_SECRET_KEY = '你的Secret Key'
OKX_PASSPHRASE = '你的Passphrase'
```

API密钥只需开通**读取**权限。

### GitHub Actions Secrets

在仓库 Settings → Secrets and variables → Actions 添加：

| Name | Value |
|:-----|:-------|
| `OKX_API_KEY` | 你的API Key |
| `OKX_SECRET_KEY` | 你的Secret Key |
| `OKX_PASSPHRASE` | 你的Passphrase |

---

## 三、核心命令

### 全量扫描（最常用）

```bash
python quant_factors/run_daily_picks.py --top 40
```

### 只看指定币种

```bash
python quant_factors/run_daily_picks.py --coins BCH,LPT,UNI
```

### 调整过滤参数

```bash
python quant_factors/run_daily_picks.py --top 50 --min-r1 2.0 --min-oi 1000000
```

---

## 四、系统架构

### 数据流

```
OKX API → 15分钟K线(200根) + 日线K线(200根)
              ↓
       build_features_single() → 88项技术特征
              ↓
       88因子评估器 × 99交易员档案 → KOL投票
              ↓
       双周期方向判断 + ADX趋势 + 通道位置
              ↓
       大盘环境过滤 + 方向一致性过滤 + ADX过滤 + TP1利润过滤
              ↓
       ★ 最终推荐 + 分批入场方案
```

### 数据源

| 数据 | 来源 | 参数 |
|:----|:-----|:------|
| 15分钟K线 | OKX REST API | 200根（≈50小时） |
| 日线K线 | OKX REST API | 200根 |
| 资金费率 | OKX public API | 实时 |
| 持仓量 | OKX public API | 实时 |
| 最大杠杆 | OKX instruments API | 各币种最大允许杠杆 |
| 交易员档案 | profiles_v2/ | 99份JSON |
| 88个因子 | quant_factors/capabilities/ | 13个评估器文件 |

### 88因子分类

| 类别 | 数量 | 文件 | 作用 |
|:----|:----:|:-----|:------|
| 技术指标 | 9 | indicators.py | RSI背离/金叉死叉/布林带/Fib/均线收复 |
| 形态识别 | 22 | patterns.py | 楔形/旗形/头肩顶/双顶底/三角收敛 |
| 结构性偏斜 | 8 | structural.py | 艾略特波浪/Wyckoff/SMC/ICT |
| 周期因子 | 2 | cycle.py | 4年周期/减半周期 |
| 市场状态 | 5 | regime.py | 趋势/盘整/高波动/抛物线衰竭 |
| 宏观相关性 | 7 | macro.py | DXY/SPX/黄金/M2/PMI |
| 衍生品信号 | 7 | derivatives.py | 资金费率/持仓量 |
| 链上信号 | 5 | onchain.py | 交易所流入/LTH/NVT/MVRV |
| 风险管理 | 3 | risk.py | 别接飞刀/仓位管理/止损 |
| 事件驱动 | 2 | events.py | FOMC/ETF流量 |
| 涌现因子 | 18 | emerged.py | Keltner均值回归/200WMA/月度季节 |

### 99交易员档案

每个交易员JSON包含：
- `handle` — Twitter账号
- `school_primary` — 交易流派（技术/链上/宏观/混合等）
- `bias_default` — 默认偏多/偏空/中性
- `capabilities_used` — 使用的因子列表+权重+证据

---

## 五、筛选流水线

```
OKX前40币（按24h成交量）
      ↓
基础过滤: R1涨幅≥1.5% + OI≥600K USDT
      ↓
大盘环境过滤:
  方向一致币中，做空>做多×2 → 🔴偏空 → 不做多
  方向一致币中，做多>做空×2 → 🟢偏多 → 不做空
  否则 → ⚪均衡 → 两边都做
      ↓
方向一致性检查: 15mKOL方向 == 日KOL方向？
  ✅一致 → 继续
  ⚠️冲突 → 排除
      ↓
ADX检查: ≥25？
  ✅有趋势 → 继续
  ❌震荡 → 排除
      ↓
TP1利润检查: 涨幅% × 杠杆 ≥ 100%？
  ✅能翻倍 → ★最终推荐
  ❌不能 → 排除
```

---

## 六、输出格式

### 6.1 终端输出（三区块）

---

#### 区块1：全量排名

```
═══ 全量排名 ═══
排序: 日KOL看多组(ADX降序) → 日KOL看空组(ADX降序)
✅=方向一致  ⚠️=方向冲突

序 币种 方向 强度 方向 小时 位置    RSI 入场价   止盈价   杠杆 TP1利润% 15mKOL 日KOL   偏度
──────────────────────────────────────────────────────────────────────────────
 1 LIT   🟢 68.2 ↑      🟡偏高  74 $2.2692 $2.3662 50x    214% 88/4  75/9   +0.17 ✅
 2 ETH   🟢 44.6 ↑      🟡偏高  70 $1970.0 $2017.0 100x   239% 91/4  75/11  +0.19 ✅
...
```

| 列名 | 说明 |
|:----|:------|
| 序 | 组内排名（看多组先排，看空组后排） |
| 币种 | 交易对 |
| 方向 | 🟢多/🔴空（该币的推荐交易方向） |
| 强度 | ADX值，≥25强趋势，越高越强 |
| 方向(箭头) | ↑ADX上升(趋势增强)/↓ADX下降(趋势减弱) |
| 小时 | 预计ADX跌破25的小时数（空=不会跌破） |
| 位置 | Keltner通道位置：🔴上轨上/🟡偏高/🟢中位/🔵偏低/🟣下轨下 |
| RSI | 相对强弱指数，>70超买/<30超卖 |
| 入场价 | 当前价格 |
| 止盈价 | TP1目标价：做多=R1阻力位，做空=S2支撑位 |
| 杠杆 | OKX该币种最大允许杠杆 |
| TP1利润% | 涨幅%×杠杆，≥100%即翻倍 |
| 15mKOL | 15分钟KOL投票：看多人数/看空人数 |
| 日KOL | 日线KOL投票：看多人数/看空人数 |
| 偏度 | KOL加权平均偏度，负=偏空，正=偏多 |
| ✅/⚠️ | ✅=双周期一致 / ⚠️=方向冲突 |

---

#### 区块2：今日推荐

```
═══ 今日推荐 ⚪ 市场均衡 ═══
（大盘环境显示：🔴偏空=不做多 / 🟢偏多=不做空 / ⚪均衡=两边都做）

── 做空推荐 ──
序 币种  强度 方向 小时 位置   RSI 入场价   止盈价   杠杆 TP1利润% 15mKOL 日KOL
────────────────────────────────────────────────────────────────────────
 1 WLD  66.0 ↑     🔵偏低  29 $0.3202 $0.2923 50x    436% 2/94  21/72

── 做多推荐 ──
序 币种  强度 方向 小时 位置   RSI 入场价   止盈价   杠杆 TP1利润% 15mKOL 日KOL
────────────────────────────────────────────────────────────────────────
 1 KAITO 29.2 ↓ 2h 🟢中位  58 $1.3142 $1.4518 50x    523% 92/1  96/1
```

---

#### 区块3：分批入场参考

```
── 分批入场参考 ──
▶ WLD 做空
  入场1 $0.325400(20U) → 入场2 $0.328000(30U) ORB上沿
  止损 $0.331257 | TP1 $0.298400
▶ KAITO 做多
  入场1 $1.3142(20U) → 入场2 $1.2919(30U) ORB下沿 | 趋势 2h
  止损 $1.2905 | TP1 $1.4518
```

| 批次 | 仓位 | 入场点 |
|:----|:----:|:-------|
| 入场1 | 20U | 信号确认后立即入场 |
| 入场2 | 30U | 回撤到ORB区间边界（做多=下沿，做空=上沿） |
| 止损 | — | 强平价 + 0.2%缓冲（自动计算，入场2自动clip在止损之上） |
| TP1 | — | 做多=R1，做空=S2 |

---

### 6.2 Word文档

每小时自动生成横板Word报告，保存在Actions的Artifacts中。

**文档结构：**

| 区块 | 内容 | 列数 |
|:----|:-----|:----:|
| 区块1 | 报告头（大盘环境 + 时间 + 扫描统计） | — |
| 区块2 | 全量排名表 | 15列 |
| 区块3 | 🟢做多推荐表 | 15列（含入场1/加仓/止损/止盈） |
| 区块4 | 🔴做空推荐表 | 15列 |
| 区块5 | 分批入场参考（纯文字） | — |
| 区块6 | 列注释说明 | — |

---

### 6.3 GitHub Actions页面

每次运行后，Actions页面顶部直接显示完整终端输出（全量排名+推荐+分批入场），不用下载。

同时Artifacts区提供Word报告和JSON数据下载，保留7天。

---

## 七、仓位管理

| 批次 | 仓位 | 占总资金 | 入场条件 |
|:----|:----:|:--------:|:---------|
| 入场1 | 20U | 20% | 信号确认立即入 |
| 入场2 | 30U | 30% | 回撤到ORB区间边界 |
| 止损 | — | — | 强平价 + 0.2%缓冲 |

> 入场2价格自动clip在止损之上，不会出现"到了也加不了仓"的情况。

---

## 八、交易纪律

```
入场 → 硬扛到TP1（利润=本金）
TP1到 → 平50%收回本金 → 剩余半仓成本=0
回来找系统 → 重新判断剩余仓位的止盈/止损
```

---

## 九、文件结构

```
suoyao-quant/
├── .github/workflows/hourly_scan.yml   ★ GitHub Actions配置（每小时自动跑）
├── .gitignore
├── TEMPLATE.md                         输出格式模板
├── 系统使用说明.md                     完整文档
├── quant_factors/
│   ├── run_daily_picks.py              ★ 主入口 — 一键全量扫描（849行）
│   ├── run_5m_kol_consensus.py          5分钟山寨币全量扫描（备用）
│   ├── entry_timing.py                  入场时机分析
│   ├── mtf_signal.py                    多时间框架报告
│   ├── smc_entry_signal.py              SMC结构分析
│   ├── okx_data_adapter.py              OKX数据接口层
│   ├── feature_engine.py                特征工程
│   ├── local_config.py                  API密钥配置
│   ├── capabilities/                    88个因子评估器（13个文件）
│   │   ├── registry.py                  因子注册中心
│   │   ├── indicators.py                技术指标（9个）
│   │   ├── patterns.py                  形态识别（22个）
│   │   ├── structural.py                结构性偏斜（8个）
│   │   ├── cycle.py                     周期因子（2个）
│   │   ├── regime.py                    市场状态（5个）
│   │   ├── macro.py                     宏观相关性（7个）
│   │   ├── derivatives.py               衍生品信号（7个）
│   │   ├── onchain.py                   链上信号（5个）
│   │   ├── risk.py                      风险管理（3个）
│   │   ├── events.py                    事件驱动（2个）
│   │   └── emerged.py                   涌现因子（18个）
│   └── 其他辅助脚本
├── profiles_v2/                         99个交易员档案（99个JSON）
├── ohlc_daily.json                      日线OHLC数据
├── macro_daily.json                     宏观数据
└── capabilities_v1.json                 470条能力库
```

---

## 十、每列注释

| 列名 | 说明 |
|:----|:------|
| 序 | 组内排名序号 |
| 币种 | 交易对 |
| 方向 | 🟢多=推荐做多, 🔴空=推荐做空 |
| 强度 | ADX趋势强度，≥25为强趋势 |
| 方向(箭头) | ↑ADX上升(趋势增强)/↓ADX下降(趋势减弱) |
| 小时 | 预计ADX跌破25的小时数（空=短期不会跌破） |
| 位置 | Keltner通道位置：🔴上轨上/🟡偏高/🟢中位/🔵偏低/🟣下轨下 |
| RSI | 相对强弱指数，>70超买，<30超卖 |
| 入场价 | 当前价格，信号确认时参考 |
| 加仓 | 入场2价格，ORB区间边界（自动clip在止损之上） |
| 止损 | 强平价+0.2%缓冲 |
| 止盈价 | TP1目标价：做多=R1阻力位，做空=S2支撑位 |
| 杠杆 | OKX该币种最大允许杠杆 |
| TP1利润% | 到TP1的利润=涨幅%×杠杆，≥100%即翻倍 |
| 15mKOL | 15分钟K线KOL投票：看多人数/看空人数 |
| 日KOL | 日线KOL投票：看多人数/看空人数 |
| 偏度 | KOL加权平均偏度，负=偏空，正=偏多 |
