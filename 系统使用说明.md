# 锁妖塔量化交易系统 — 使用说明

> 版本: v2.5 | 最后更新: 2026-07-30

---

## 一、系统概述

把 99 个顶级加密交易员的经验炼成 88 个量化因子，对 OKX 山寨币做全量扫描、排名和推荐。

**三重时间框架 + 审判系统交叉验证 + 精准入场 + 回测。**

**核心理念：**
- 88因子 × 99KOL = 三重框架实时投票
- 15m + 1H + 日线三重周期过滤，1H 作为**方向仲裁者**
- 审判系统 5 个独立检测器交叉验证
- 精准入场规划（ICT OTE + 海龟回撤）
- 每次推荐自动回测上次结果（逐K线回放，精确判断TP1/爆仓先后）
- KOL 绩效自动追踪（贝叶斯平滑，持续预测错误的KOL自动降权）

**运行频率：每 4 小时跑一次（CI 自动）**

---

## 二、快速开始

```bash
# 一键全量扫描+推荐+审判+入场+回测
python quant_factors/run_daily_picks.py

# 指定币种
python quant_factors/run_daily_picks.py --coins BCH,LPT,UNI

# 指定参数
python quant_factors/run_daily_picks.py --top 30 --min-r1 2.0
```

### GitHub Actions 自动运行

`.github/workflows/hourly_scan.yml` — 每小时第 5 分钟自动执行。

---

## 三、输出解读

### 核心输出顺序（手机适配）

```
1. 综合推荐 Top 3       ← 第一眼就看到
2. 精准入场方案          ← 第二眼
3. 回测上次推荐          ← 第三眼（胜率/盈亏比/均盈）
4. 锁妖塔扫描详情        ← 全量排名/推荐/分批入场
5. 审判系统验证          ← 5个检测器评分/分歧币种
```

### 扫描输出格式

```
ONDO 🔴 23.9 → 🟢中位 49 $0.3963 50x 4035% 22/64 11/74 92/2 ✅LTF+MT
  ↑    ↑    ↑   ↑   ↑     ↑     ↑    ↑    ↑     ↑     ↑     ↑     ↑
币种 方向 ADX 趋势 位置 RSI  入场价 杠杆 TP1% 15mKOL 1HKOL 日KOL 对齐度
```

### 对齐度说明

| 对齐度 | 分数 | 含义 |
|---|---|---|
| 三重一致 | 1.0 | 三层全同意 → 最高置信度 |
| MTF+HTF一致 | 0.8 | 中间层和大趋势同意 |
| LTF+MTF一致 | 0.6 | 短中期同意，日线分歧 |
| LTF+HTF一致 | 0.3 | 跳过中间层，信心低 |
| 三向分歧 | 0.0 | 不推荐 |

---

## 四、系统架构

```
OKX API → 88因子特征 → 99KOL投票 → 三重框架扫描(15m+1H+日线)
    ↓
对齐度≥0.6 + ADX≥25 + TP1利润≥100% → 综合推荐 Top 3
    ↓
审判系统 5 检测器交叉验证（CVD/FVG/流动性/情绪）
    ↓
精准入场方案（ICT OTE 第一入场 20U + 海龟回撤 第二入场 30U）
    ↓
回测上次推荐（胜率/盈亏比/均盈）
```

### 关键配置

| 参数 | 值 | 说明 |
|---|---|---|
| LTF | 15m×200根 | 入场时机 |
| MTF | 1H×168根 | 趋势仲裁 |
| HTF | 日线×200根 | 大趋势 |
| 仓位 | 20U+30U | 分批入场 |
| 杠杆 | OKX最大值 | API实时获取 |
| TP1 | entry×(1±1/lev) | 利润=本金 |
| 强平 | 理论×70% | 维持保证金 |

---

## 六、v2.5 8维位置共识（2026-07-30）

用 8 个独立维度替代单一 Keltner 通道判断"偏高/偏低"：

| 维度 | FMZ 参考策略 | 核心算法 |
|---|---|---|
| Keltner | 已有 | `(close - kc_lower) / (kc_upper - kc_lower)` |
| Bollinger %B | BB Mean-Reversion w/ Dynamic-Support | `(close - bb_lower) / (bb_upper - bb_lower)` |
| MA50 偏离 | Dual-MA-Deviation + ATR Trend | `(close/ma50 - 1)` 归一化 |
| MA200 偏离 | 200-EMA-VWAP-MFI Trend | 山寨币价值区判断 |
| 20 日区间 | 5-day High-Low Breakout Channel | `(close - low_20d) / (high_20d - low_20d)` |
| Pivot 区间 | Dynamic-S/R Adaptive Pivot | `(close - S1) / (R1 - S1)` |
| RSI 位置 | RSI Overbought/Oversold + BB | 动量位置代理 |
| Fib 回撤 | RSI + Fibonacci Retracement | 摆动高低点相对位置 |

**输出升级**：`🟡偏高(5/8)` — 共识中位数 + 偏高维度的数量，一目了然。
中位但 5+ 维度说偏高时显示 `🟢中位⚠️(5/8偏高)` — 警惕隐藏风险。
综合评分从四维→五维，位置共识占 20% 权重。

### 新增文件
- `quant_factors/position_gauges.py` — 8 个位置判断器 + 共识聚合

---

## 七、v2.4 FMZ 策略融合（2026-07-30）

基于 FMZ 策略广场 5807 个策略的对比分析，对 5 个核心模块做了对标升级：

### ① 因子评估升级
- 88 因子从手工 if-else 退化版 → 接入 `CAP_REGISTRY` 向量化评估器
- `kol_vote()` 优先使用 `evaluate_all()` 全 DataFrame 批量计算
- mock 因子（18个）自动回退 cscore，不影响现有逻辑
- 形态检测（楔形/旗形/艾略特等）不再退化为 `c > ma50`，真正检测旗杆+波动率压缩+突破

### ② 5 检测器对标 FMZ 原版升级
| 检测器 | FMZ 原版 | 升级内容 |
|---|---|---|
| CVD 背离 | #444980 | +隐藏背离（2→4种），补回 Bull/Bear Power 趋势延续信号 |
| FVG 缺口 | #452736 | +FVG中点/类型元数据，供入场规划器使用 |
| MTF 扫荡 | #488279 | HTF 窗口 24h→96h（模拟真正 4H 级别） |
| 流动性级联 | #483051 | 静态阈值 8%/15%→89周期历史极值动态判+连续确认 |
| 情绪震荡器 | Sentiment-Oscillator | +MACD柱+信号线交叉（FMZ 核心交易信号） |

### ③ 回测逐 K 线回放
- `check_path_kbar()` 替代原 `check_path()`（只看 24H 高低点）
- 推荐时保存 15m K 线快照到日志，回测时从锚点逐根判断
- 精确判断 TP1 和爆仓的先后顺序（原版两者都到总是判 TP1）
- 无快照时自动回退到原 24H 高低点方法

### ④ KOL 绩效追踪
- 新模块 `quant_factors/kol_tracker.py`
- 每个 KOL 独立追踪投票准确率
- 贝叶斯平滑：`weight = (correct + 1) / (total + 2)`
- 回测结果（到 TP1/爆仓）驱动自动升降权重
- 权重介入 `kol_vote()` 信号计算，差 KOL 自动降权

### ⑤ OTE + 海龟入场改进
- `calc_ote_levels()` 从全局 max/min → 局部极值检测摆动点
- `calc_turtle_pullback()` 自适应 K 线数量，标注正宗海龟需日线数据

---

## 七、之前修复（v2.1→v2.3）

### 炼妖塔——10 个导致推荐反指的 bug 已修复

| 修复 | 说明 |
|---|---|
| 中性默认做空 | 三路分支，中性跳过 |
| 情绪检测器失效 | K线需求从250→60根 |
| Pivot来自15m | 改用日线high/low |
| ADX两套实现 | 统一为calc_adx |
| API错误静默 | 全部加日志 |
| 趋势强度clamp | aligned+raw双值 |
| 数据列名不一致 | normalize_ohlc_df共享 |
| 三重时间框架 | 新增1H KOL + 对齐度评分 |

### 审判系统——12 个问题已修复

- 分歧阈值从 2.0 改为 0.5（死代码复活）
- 5 个检测器按数据周期自动调整窗口（`bars_per_unit`）
- EMA 统一到基类（删除 3 处重复）
- 置信度公式不再被中性检测器拉低
- 10 个死配置标注+删除

### 回测系统——重写（v2.3）

- TP1 改用 entry_planner 精准计算值
- ~~24H 高低点判断价格路径~~ → v2.4 升级为逐K线回放
- 仓位从 20U 改为 50U（两单合计）
- 新增胜率/盈亏比/均盈指标
- 记录加 ADX/RSI/对齐度/KOL 上下文

---

## 八、GitHub

**https://github.com/davidlu020106-tech/suoyao-quant**

代理配置：
```bash
git config --global http.proxy http://127.0.0.1:7892
```

OKX API 密钥在 GitHub Secrets 中。
