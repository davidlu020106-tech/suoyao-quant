# 锁妖塔量化交易系统 — 使用说明

> 版本: v3.0 | 最后更新: 2026-07-30

---

## 一、系统概述

88 因子 × 99 KOL → 三重时间框架投票 → 审判交叉验证 → 四维综合评分 → 精准入场 + 回测。

日线 KOL 为尊（50%权重），1H 和 15m 辅助。核心不变量：**推荐只提供信号，不自动下单。**

**新增模块：**
- 12维位置共识（滚动百分位+速度）
- 20维超级趋势识别（纯标签不影响判断）
- KOL绩效追踪（贝叶斯平滑自动升降权重）
- 回调/反弹机会（绕过锁妖塔过滤独立推荐）

---

## 二、快速开始

```bash
python quant_factors/run_daily_picks.py                 # 全量扫描
python quant_factors/run_daily_picks.py --coins BCH,LPT  # 指定币种
python quant_factors/run_daily_picks.py --top 30         # 指定数量
```

CI: `.github/workflows/hourly_scan.yml` 每小时第5分钟自动运行。

---

## 三、推荐流程

```
40币 → 基础过滤(R1≥1.5%+OI≥600K) → result列表
  ↓
passed过滤:
  ① alignment ≥ 0.3 (15m+1H同向即可, 日线反对不排除但KOL共识大幅降权)
  ② ADX ≥ 20
  ③ 做多pos≥75且恶化中 → 排除; 做空pos≤25且恶化中 → 排除
  ↓
综合评分 Top 3 (四维):
  锁妖塔 40% + 审判 25% + 趋势 20% + 位置 15%
  分歧(审判反对锁妖塔) → 降权×0.5
  位置D/F级 → 降权×0.3
  ↓
回调/反弹机会 (纯位置+日线, 不过过滤):
  日线看多 + pos 26-40 → 回调买入
  日线看空 + pos 60-74 → 反弹做空
```

### 锁妖塔 p_score 公式

```
p_score = ADX/50 × 0.25 + alignment × 0.25 + KOL共识 × 0.30 + TP1/500 × 0.20

KOL共识 = 日线极化度×0.50 + 1H×0.30 + 15m×0.20
方向惩罚: 15m反对日线×0.7, 1H反对日线×0.5
```

---

## 四、输出解读

### 综合推荐 Top 3
```
1. AEON   🟢做多  评分=0.78  ADX=48  价格=$0.11  位置=23(-8)  TP1=$0.18  三重一致  ✅一致
```
格式: 币种 方向 评分 ADX 价格 位置(偏向) TP1 对齐度 标签

### 全量排名表
```
序 币种  超级趋势  方向 强度 位置 RSI 入场价 止盈价 杠杆 TP1% 15mKOL 1HKOL 日KOL 对齐
 1 OM    🔥超级做空52  🔴 51 🟡偏高77(+16) 36 $0.44 $0.00 50x 3617% 20/54 20/54 20/54 三重一致
```

### 位置显示格式
```
🟣极低23(-8) ↓12    — 23分, 8个维度偏低, 速度↓12(改善中)
🟡偏高77(+16) ↑10   — 77分, 16个维度偏高, 速度↑10(恶化中)
🟢中位50(±0)         — 中性
```

### 超级趋势标签
```
🔥超级做多52 → 多头得分≥50
📈做多38     → 多头≥35
📉做空36     → 空头≥35
多12/空8     → 微弱信号
11           → 得分太低
```

---

## 五、新模块

### 12维位置共识 (`position_gauges.py`)
滚动百分位+速度，去冗余12维度:
| Keltner | Bollinger | ATR | 10日区间 | 20日区间 | 50日区间 |
| SMA10 | MA50 | MA200 | RSI | Pivot | Fib |

裁尾均值(去最高最低各2个) → 0-100分 + 速度(正=恶化/负=改善) + A~F等级

### 20维超级趋势 (`super_trend.py`)
纯标签不影响方向判定，参考20个FMZ策略:
| ADX强度 | ADX方向 | DI差值 | SuperTrend | SAR | CCI | Aroon | Vortex |
| 一目云 | Donchian | MA排列 | MACD柱 | 成交量 | BB突破 | RSI | OBV | 多周期 | 突破力度 | 资金费率 |

≥50分🔥超级, ≥35分📈强, 否则显示得分

### KOL绩效追踪 (`kol_tracker.py`)
每个KOL独立追踪投票准确率，贝叶斯平滑 `(correct+1)/(total+2)`。回测驱动更新。

---

## 六、审判系统

5检测器 → 加权聚合 → 方向判定(threshold 0.06):
- CVD背离(4种: 常规+隐藏)
- FVG缺口(中点+类型元数据)
- 多TF扫荡(HTF窗口96h)
- 流动性级联(89周期动态极值+连续确认)
- 情绪震荡器(MACD柱+信号线交叉)

**只审判passed列表中的币**, 不全量扫描。

---

## 七、回测

逐K线回放: 推荐时保存15m K线快照 → 回测时逐根判断TP1/爆仓先后。无快照时回退到24H高低点。

---

## 八、文件结构

```
suoyao-quant/
├── quant_factors/
│   ├── run_daily_picks.py          ★ 主入口
│   ├── okx_data_adapter.py         OKX数据+特征工程
│   ├── position_gauges.py          ★ 12维位置共识
│   ├── super_trend.py              ★ 20维超级趋势
│   ├── kol_tracker.py              ★ KOL绩效追踪
│   ├── entry_timing.py             入场时机
│   ├── smc_entry_signal.py         SMC结构+ADX
│   ├── capabilities/               88因子
│   └── ...
├── judge_system/
│   ├── judge_engine.py             审判引擎
│   ├── detector_registry.py        注册+聚合
│   ├── entry_planner.py            精准入场(OTE+海龟)
│   ├── backtest_log.py             回测(逐K线)
│   ├── detectors/                  5个检测器
│   └── ...
├── profiles_v2/                    99个KOL档案
└── .github/workflows/hourly_scan.yml
```

---

## 九、GitHub

https://github.com/davidlu020106-tech/suoyao-quant

代理：`git config --global http.proxy http://127.0.0.1:7892`
OKX密钥在 GitHub Secrets。
